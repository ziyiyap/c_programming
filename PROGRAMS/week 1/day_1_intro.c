#include <stdio.h>
//similar to python's import

int main() { //main() is a must, the whole program searches a function called main(). Basically the key of all C programs
    printf("Hello, World\n"); //Must be doubled quoted, and end with ; before newline. PS: \n is optional
    return 0;
}